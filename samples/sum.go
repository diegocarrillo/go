package main

import(
  "fmt"

)

func main()  {
    var num int = 10
    var sum int = 10 * 10 //we can define an operation inside a var

    fmt.Print(num * 5) //We use * for multp and / for division
    fmt.Print("\n")
    fmt.Print(sum)
}
